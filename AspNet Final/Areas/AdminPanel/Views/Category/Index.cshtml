@model AspNet_Final.Areas.AdminPanel.Controllers.CategoryController

    @{
        ViewData["Title"] = "Index";
        //int count = 1;
    }
    <div class="row">
        <div class="col-12 my-3">
            <a asp-action="Create" class="btn btn-success">Create</a>
        </div>
        <div class="col-lg-12 ion-grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Categories</h5>
                    <table class="mb-0 table table-dark">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Category name</th>
                                <th>IsDeleted</th>
                                <th>Setting</th>
                            </tr>
                        </thead>
                        <tbody>
                            @*@foreach (var cg in Model)
                            {
                                <tr>


                                    <td>@count</td>
                                    <td>@cg.Name</td>
                                    <td>@(cg.IsDeleted?"Deleted":"Active")</td>
                                    <td>
                                        <a asp-action="Detail" asp-root-id="@cg.Id" class="btn btn-info">
                                            <i class="fa-address-card"> </i>
                                        </a>
                                        <a asp-action="Update" asp-root-id="@cg.Id" class="btn btn-warning">
                                            <i class="fa-upload"> </i>
                                        </a>
                                        <a asp-action="Delete" asp-root-id="@cg.Id" class="btn btn-danger">
                                            <i class="@(cg.IsDeleted?"fa-check":"fa-delete" ) ">
                                        </a>
                                    </td>
                                </tr>
                                count++;
                            }*@

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

   